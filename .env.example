# DB 백엔드: supabase | sheets (기본값: supabase)
# 로컬 현장등록/자격증등록 → Supabase 저장 시 반드시 supabase 로 두세요.
DB_BACKEND=supabase

# Supabase (DB_BACKEND=supabase 일 때 필수)
# 대시보드 → Project Settings → API 에서 확인
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co

# Secret Key (백엔드용, 서버에서만 사용, 절대 공개 금지)
# 형식: sb_secret_... 또는 service_role 키
# 사용처: api/services/supabase_service.py, Flask API 서버
# 주의: 이 키는 절대 브라우저에 노출되면 안 됩니다!
SUPABASE_KEY=your_service_role_secret_key

# Publishable Key / Anon Key (프론트엔드용, 브라우저 노출 가능)
# 형식: sb_publishable_... 또는 JWT 형식 (eyJhbGci...)
# 사용처: js/supabaseClient.js, js/config.js 등 프론트엔드
# 보안: RLS 정책이 설정되어 있으면 공개적으로 공유 가능
SUPABASE_ANON_KEY=your_publishable_or_anon_key

# Vite 사용 시 브라우저에서 쓰려면: VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY

# PostgreSQL 직접 연결 (supabase_migrate.py, db_connect_test.py)
# DATABASE_URL 있으면 우선 사용. 없으면 아래 user/password/host 등 사용
# DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.hhpofxpnztzibtpkpiar.supabase.co:5432/postgres
user=postgres
password=your_db_password
host=db.hhpofxpnztzibtpkpiar.supabase.co
port=5432
dbname=postgres

# Google Sheets (DB_BACKEND=sheets 일 때만 사용)
# SPREADSHEET_ID=15fAEzkC9FCLA6sG1N--f69r-32WHoYLvmXcwED5xWzM
# SHEET_SITES=시트1
# SHEET_PERSONNEL=시트2
# SHEET_CERTIFICATES=시트3

# === Flask API 서버 설정 ===
FLASK_PORT=5000
FLASK_DEBUG=True

# === API 연결 설정 ===
# Streamlit이 Flask API를 호출할 때 사용하는 URL
# 로컬 개발: http://localhost:5000 (기본값)
# 배포 시: https://your-api-server.com (Flask API가 다른 서버에 있는 경우)
# Streamlit Cloud 배포 시: Streamlit Secrets에 설정 필요
API_BASE_URL=http://localhost:5000

# === CORS 설정 ===
# 허용할 오리진 목록 (쉼표 구분, 공백 없이)
# 로컬 개발: 기본값 사용 (localhost, 127.0.0.1, 포트 5000/8000/8501)
# 배포 시: 프로덕션 도메인 추가 필요
# 예: ALLOWED_ORIGINS=http://localhost:5000,http://localhost:8501,https://your-app.streamlit.app,https://your-web-domain.com
ALLOWED_ORIGINS=http://localhost:5000,http://127.0.0.1:5000,http://localhost:8000,http://127.0.0.1:8000,http://localhost:8501,http://127.0.0.1:8501

# 프로덕션 도메인 추가 (쉼표 구분, ALLOWED_ORIGINS에 자동 추가됨)
# 예: PRODUCTION_ORIGINS=https://your-app.streamlit.app,https://your-web-domain.com
# PRODUCTION_ORIGINS=

# === 정적 파일 서빙 설정 ===
# Flask가 정적 파일(HTML, CSS, JS)을 서빙할지 여부
# 로컬 개발: true (기본값)
# 프로덕션: false (Nginx 등 웹 서버가 정적 파일을 직접 서빙하는 경우)
# SERVE_STATIC_FILES=true
